let arr = [ 9, 4, 4, 2, 2, 4, 4, 3, 5];

let num = 5;

//element larger than a number num 
function getElements(arr, num){
  for(let i = 0; i < arr.length; i++){
    if (arr[i] > num ) {
      console.log(arr[i]); g

    }
  }
}
getElements(arr, num);

// js file retuen array element larger than number 
